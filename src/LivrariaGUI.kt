import javafx.application.Application
import javafx.scene.Scene
import javafx.scene.control.Button
import javafx.scene.layout.VBox
import javafx.stage.Stage

/*class LivrariaGUI : Application() {
    override fun start(primaryStage: Stage?) {
        val btnListarLivros = Button("Listar Livros")
        btnListarLivros.setOnAction { Livraria().listarLivrosDisponiveis() }

        val btnListarFuncionarios = Button("Listar Funcionários")
        btnListarFuncionarios.setOnAction { Livraria().listarFuncionarios() }

        val layout = VBox(btnListarLivros, btnListarFuncionarios)
        val scene = Scene(layout, 300.0, 200.0)

        primaryStage?.title = "Livraria"
        primaryStage?.scene = scene
        primaryStage?.show()
    }
}*/
